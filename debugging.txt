# Rejoin Engine - FIX v0.4.4 (satu file, PASTI bisa)
#
# Masalah: tool selama ini mengirim link HTTPS asli (https://www.roblox.com/games/...)
# bukan deep link roblox://placeId=<id>, makanya selalu kebuka halaman game.
# Fixed: sekarang managers/recovery.lua BANGUN sendiri link roblox://placeId=<id>
# dari privateServer (tanpa bergantung file lain).

# ============================================================
# STEP 1 - SALIN 1 FILE dari PC ke device:
#   managers/recovery.lua   ->   ~/rejoin/managers/recovery.lua
# (PASTIKAN tertimpa, bukan diskip)

# ============================================================
# STEP 2 - VERIFIKASI file recovery.lua yang baru ada di device (harus menghasilkan "extractPlaceIdLenient"):
grep -n "extractPlaceIdLenient" managers/recovery.lua

# Kalau output KOSONG -> file belum tertimpa, ulangi STEP 1.
# Kalau ada baris -> file sudah versi baru, lanjut STEP 3.

# ============================================================
# STEP 3 - JALANKAN TOOL (Launch All = opsi 1), lalu lihat log:
#   data/rejoin.log (atau cek langsung saat monitor jalan)
# Baris penentu, HARUS "roblox://placeId=..." (BUKAN https):
#   Recovery: opening game link for <name>: roblox://placeId=107778070777162
#   Android: opening URL roblox://placeId=107778070777162 (pkg=com.apengjers.vN)

# Jika log sudah menunjukkan roblox://placeId=... dan tetap halaman game,
# kabari saya hasilnya (dengan log + apa yang terlihat di layar).
